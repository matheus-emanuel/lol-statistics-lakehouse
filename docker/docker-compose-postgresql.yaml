services:
  db:
    image: postgres:16.10-alpine3.22
    container_name: lol_data_DW
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "iceberg" ]
      interval: 5s
      retries: 10
    environment:
      POSTGRES_PASSWORD: postgresql
      POSTGRES_USER: postgresql
      POSTGRES_DB: lol_data_DW
    ports:
      - "5432:5432"
    volumes:
      - ../data/lol_data_DW:/var/lib/postgresql/data
